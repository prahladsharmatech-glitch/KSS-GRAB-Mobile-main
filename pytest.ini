[pytest]
; Ensures tests always resolve `from backend.app.main import ...` correctly,
; regardless of whether `pytest` is invoked from the project root or from
; inside backend/. Previously, running `pytest` from inside backend/ caused
; "ModuleNotFoundError: No module named 'backend'".
testpaths = backend/tests
pythonpath = .
asyncio_mode = strict
